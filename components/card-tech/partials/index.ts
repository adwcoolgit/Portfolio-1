import { RoundedIcon } from '@/components/rounded-icon';
import { Wrapper } from '@/components/wrapper';
import { TechCard } from './card';

export const UITechCard = {
  Wrapper: Wrapper,
  Card: TechCard,
  Icon: RoundedIcon,
};
